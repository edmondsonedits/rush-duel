const held={key:{left:false,right:false,down:false,horizontal:null},touch:{left:false,right:false,down:false,horizontal:null}},repeat={key:{left:0,right:0,down:0},touch:{left:0,right:0,down:0}},activePointers=new Map();
const DAS=115,ARR=32,SOFT=22;
const softDropButton=document.querySelector('[data-action="down"]'),softDropHint=softDropButton?.querySelector('small');
function applyAction(action,{predicted=false}={}){
  if(!game.started||activeScreen!=='game')return false;if(game.mode==='online'&&action==='pause')return false;if(game.mode==='online'&&network.role==='guest'&&!predicted)return network.sendInput(action);
  let changed=false;if(action==='left')changed=game.move(game.player,-1,0);else if(action==='right')changed=game.move(game.player,1,0);else if(action==='down')changed=game.move(game.player,0,1,true,performance.now());else if(action==='ccw')changed=game.rotate(game.player,false);else if(action==='cw')changed=game.rotate(game.player,true);else if(action==='drop'){navigator.vibrate?.(8);changed=game.commit('player');}else if(action==='pause')changed=game.togglePause();
  if(changed){if(['left','right'].includes(action))playTone('move');else if(action==='down')playTone('soft');else if(['ccw','cw'].includes(action))playTone('rotate');if(game.mode==='online'&&network.role==='host')network.broadcast(true);}return changed;
}
function processHeld(now){for(const source of ['key','touch']){const horizontal=held[source].horizontal;if(horizontal&&held[source][horizontal]&&now>=repeat[source][horizontal]){applyAction(horizontal);repeat[source][horizontal]=now+ARR;}if(held[source].down&&now>=repeat[source].down){applyAction('down');repeat[source].down=now+SOFT;}}}
function releaseInputs(){for(const source of ['key','touch']){held[source].left=held[source].right=held[source].down=false;held[source].horizontal=null;}activePointers.clear();controls.forEach(button=>button.classList.remove('active'));gesture=null;}
function bindControl(button){
  const action=button.dataset.action,repeatable=['left','right','down'].includes(action);button.addEventListener('pointerdown',event=>{event.preventDefault();button.setPointerCapture?.(event.pointerId);button.classList.add('active');applyAction(action);if(repeatable){held.touch[action]=true;if(action==='left'||action==='right')held.touch.horizontal=action;repeat.touch[action]=performance.now()+DAS;activePointers.set(event.pointerId,action);}});
  const stop=event=>{const actionHeld=activePointers.get(event.pointerId);if(actionHeld){held.touch[actionHeld]=false;activePointers.delete(event.pointerId);if(held.touch.horizontal===actionHeld){const remaining=[...activePointers.values()].reverse().find(value=>value==='left'||value==='right');held.touch.horizontal=remaining||null;}}button.classList.remove('active');};['pointerup','pointercancel','lostpointercapture'].forEach(type=>button.addEventListener(type,stop));
}
controls.forEach(bindControl);
addEventListener('keydown',event=>{const key=event.key;if(['ArrowLeft','ArrowRight','ArrowDown','ArrowUp',' '].includes(key))event.preventDefault();if(key==='ArrowLeft'&&!held.key.left){held.key.left=true;held.key.horizontal='left';repeat.key.left=performance.now()+DAS;applyAction('left');}else if(key==='ArrowRight'&&!held.key.right){held.key.right=true;held.key.horizontal='right';repeat.key.right=performance.now()+DAS;applyAction('right');}else if(key==='ArrowDown'&&!held.key.down){held.key.down=true;repeat.key.down=performance.now()+55;applyAction('down');}else if(!event.repeat&&['z','Z','q','Q'].includes(key))applyAction('ccw');else if(!event.repeat&&['ArrowUp','x','X','e','E'].includes(key))applyAction('cw');else if(!event.repeat&&key===' ')applyAction('drop');else if(!event.repeat&&['p','P','Escape'].includes(key))applyAction('pause');});
addEventListener('keyup',event=>{if(event.key==='ArrowLeft'){held.key.left=false;if(held.key.horizontal==='left')held.key.horizontal=held.key.right?'right':null;}if(event.key==='ArrowRight'){held.key.right=false;if(held.key.horizontal==='right')held.key.horizontal=held.key.left?'left':null;}if(event.key==='ArrowDown')held.key.down=false;});addEventListener('blur',releaseInputs);
canvas.addEventListener('pointerdown',event=>{gesture={x:event.clientX,y:event.clientY};canvas.setPointerCapture?.(event.pointerId);});canvas.addEventListener('pointerup',event=>{if(!gesture)return;const dx=event.clientX-gesture.x,dy=event.clientY-gesture.y,ax=Math.abs(dx),ay=Math.abs(dy);if(ax<13&&ay<13)applyAction('cw');else if(ax>ay){const count=Math.min(4,Math.max(1,Math.round(ax/35)));for(let i=0;i<count;i++)applyAction(dx>0?'right':'left');}else if(dy<-28)applyAction('drop');else if(dy>22){const count=Math.min(5,Math.max(1,Math.round(dy/28)));for(let i=0;i<count;i++)applyAction('down');}gesture=null;});

function updateSoftDropUi(now){
  if(!softDropButton||!softDropHint)return;const localBoard=game.mode==='online'&&network.role==='guest'?game.rival:game.player,remaining=game.softDropRemaining(localBoard,now),waiting=game.phase==='active'&&remaining>0;softDropButton.style.opacity=waiting?'.58':'';softDropButton.style.filter=waiting?'grayscale(.35) brightness(.8)':'';softDropButton.setAttribute('aria-disabled',waiting?'true':'false');softDropHint.textContent=waiting?`${Math.max(.1,remaining/1000).toFixed(1)}s`:'Soft';
}
function frame(now){
  const dt=Math.min(40,now-lastFrame);lastFrame=now;if(activeScreen==='game'){
    updateSoftDropUi(now);processHeld(now);const guest=game.mode==='online'&&network.role==='guest';if(!guest){game.update(now,dt,{allowSettled:true});botAgent.update(now);}if(game.mode==='online'&&network.role==='host')network.broadcast();updateEffects(dt);render(now);
  }requestAnimationFrame(frame);
}

$('startButton').addEventListener('click',()=>beginWithTutorial(()=>showScreen('mode')));$('classicButton').addEventListener('click',()=>beginWithTutorial(beginClassic));$('botButton').addEventListener('click',()=>showScreen('bot-options'));$('onlineButton').addEventListener('click',()=>showScreen('online'));$('modeBackButton').addEventListener('click',()=>showScreen('title'));$('botBackButton').addEventListener('click',()=>showScreen('mode'));$('onlineBackButton').addEventListener('click',()=>{network.cleanup();showScreen('mode');});$('playBotButton').addEventListener('click',()=>beginWithTutorial(beginBot));$('difficultyGrid').addEventListener('click',event=>{const button=event.target.closest('[data-difficulty]');if(!button)return;selectedDifficulty=button.dataset.difficulty;difficultyCopy();playTone('move');});
$('hostButton').addEventListener('click',()=>network.host());$('joinButton').addEventListener('click',()=>network.join($('joinCode').value));$('joinCode').addEventListener('input',event=>event.target.value=event.target.value.toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,6));$('joinCode').addEventListener('keydown',event=>{if(event.key==='Enter')network.join(event.currentTarget.value);});$('copyRoomButton').addEventListener('click',async()=>{try{await navigator.clipboard.writeText(network.room);updateNetworkStatus({message:`Copied ${network.room}. Send it to your rival.`,error:false});}catch{updateNetworkStatus({message:`Room code: ${network.room}`,error:false});}});
$('exitButton').addEventListener('click',exitMatch);$('homeButton').addEventListener('click',goHome);$('againButton').addEventListener('click',playAgain);$('soundButton').addEventListener('click',()=>{soundEnabled=!soundEnabled;$('soundButton').textContent=soundEnabled?'♪':'×';if(soundEnabled)playTone('move');});$('saveScoreButton').addEventListener('click',saveScore);$('scoreName').addEventListener('keydown',event=>{if(event.key==='Enter')saveScore();});
$('tutorialButton').addEventListener('click',()=>openTutorial());$('tutorialClose').addEventListener('click',closeTutorial);$('tutorialNext').addEventListener('click',()=>{if(tutorialStep<TUTORIAL.length-1){tutorialStep++;renderTutorial();}else closeTutorial();});
addEventListener('resize',()=>{if(activeScreen==='game'){configureCanvas();render(performance.now());}},{passive:true});document.addEventListener('visibilitychange',()=>{if(document.hidden){releaseInputs();if(activeScreen==='game'&&game.started&&!game.paused&&!(game.mode==='online'&&network.role==='guest'))game.togglePause();}});

function runAppTests(){
  const core=runCoreTests(),tests=[...core.tests],check=(name,condition,detail='')=>tests.push({name,pass:!!condition,detail:condition?'':detail});
  try{
    check('single production entry',!document.documentElement.innerHTML.includes('play-v12.html')&&!document.documentElement.innerHTML.includes('document.write('));
    check('visible controls',controls.length===7&&controls.every(button=>button.textContent.trim()));
    check('worker planner available',typeof Worker==='function');
    check('online transport loaded',NetworkDuel.available(),'window.Peer missing');
    check('responsive board size',29*ROWS/620>=.93);
    check('soft-drop spawn grace is exposed',typeof game.softDropRemaining==='function'&&typeof game.canSoftDrop==='function');
    check('difficulty ordering',DIFFICULTIES.easy.forceChance<DIFFICULTIES.medium.forceChance&&DIFFICULTIES.medium.forceChance<DIFFICULTIES.hard.forceChance&&DIFFICULTIES.hard.forceChance<DIFFICULTIES.impossible.forceChance);
    const ids=[...document.querySelectorAll('[id]')].map(element=>element.id),duplicates=ids.filter((id,index)=>ids.indexOf(id)!==index);check('unique interface ids',duplicates.length===0,duplicates.join(','));
  }catch(error){check('app test runner',false,error?.message||String(error));}
  return {pass:tests.every(test=>test.pass),tests};
}

renderScores();difficultyCopy();configureCanvas();showScreen('title');const selfTest=runAppTests();window.__rushDuel={game,network,botAgent,Board,chooseBotPlan,runSelfTests:runAppTests,selfTest,applyAction,showScreen};if(new URLSearchParams(location.search).has('selftest')){$('selfTestOutput').hidden=false;$('selfTestOutput').textContent=JSON.stringify(selfTest,null,2);document.body.dataset.selftest=selfTest.pass?'pass':'fail';console.info('Rush Duel V13 self-test',selfTest);}requestAnimationFrame(frame);
